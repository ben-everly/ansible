---
platforms:
  - name: enpass
    image: geerlingguy/docker-ubuntu2404-ansible:latest
    # pin: enpass ships amd64-only packages, so test on amd64 regardless of host
    platform: linux/amd64
    privileged: true
    pre_build_image: true
